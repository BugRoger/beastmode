# Release: hook-fix

**Version:** v0.15.0
**Date:** 2026-03-08

## Highlights

Removes the `hooks` field from `plugin.json` to fix hook loading issues introduced in v0.14.35.

## Fixes

- **Hook field removed from plugin.json** — The `hooks` reference to `./hooks/hooks.json` caused issues; removed to restore correct plugin behavior
