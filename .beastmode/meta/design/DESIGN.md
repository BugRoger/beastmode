# Design Meta

Process knowledge from design phases. Key patterns: research-informed design outperforms brainstorming, fractal consistency across domains beats special-casing, HITL gate design requires structural enforcement, instruction visibility on critical paths demands visible markdown, mirroring existing algorithms constrains the design space productively, and subjective upstream gatekeeping is harmful when downstream components handle empty input gracefully.

## Process
Eleven insight clusters spanning competitive analysis, fractal consistency, HITL gate design, cross-session state, instruction visibility, scope management, L0 content scope, agent organization, external documentation drift, miscellaneous design patterns, and redundant upstream gatekeeping.

1. ALWAYS produce dated research artifacts from 3+ external sources before locking structural decisions
2. ALWAYS start from existing algorithms when building structurally analogous subsystems
3. ALWAYS seed new files from real session content, not generic templates
4. ALWAYS use visible markdown for critical-path instructions
5. ALWAYS include shared files (_shared/) in phase-scoped sweeps
6. NEVER place competing gate mechanisms on the same decision point

## Workarounds
None recorded.
